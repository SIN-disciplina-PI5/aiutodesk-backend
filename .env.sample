# Example env for running Postgres via docker-compose

# Port exposed on host -> container's 5432
DB_PORT=5432

# Database connection info (for the app running on host)
DB_HOST=localhost
DB_USER=postgres
DB_PWD=postgres
DB_NAME=aiutodesk

# Optional flags used by app config
DB_SYNC=false
DB_LOGGING=false
